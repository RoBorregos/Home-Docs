{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "python3 -m pip install -r requirements.txt -r requirements-docs.txt --break-system-packages",
  "buildCommand": "bash scripts/build.sh",
  "outputDirectory": "site",
  "env": {
    "CHATBOT_ENV": "production",
    "REQUEST_BUDGET_MS": "90000"
  },
  "functions": {
    "api/index.py": {
      "maxDuration": 120,
      "excludeFiles": "{docs/**,site/**,tests/**,.venv/**,chatbot/eval/**,scripts/**,.github/**}"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
